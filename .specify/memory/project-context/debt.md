<!-- CAP 80 lines -->
# Technical Debt — phefo

| ID | Debt | Cost of leaving it | Trigger to fix | Opened |
|---|---|---|---|---|
| TD-001 | **Partly discharged 2026-08-18.** The gate is now `cfg.sight`, defaulting to 120, so a type that must engage across height sets a number instead of inventing a mechanism (feature 003). The five base types still carry 120 and still cannot see a player on a platform. | Platforms remain safe from knifeman, swordsman, gunman, archer and brute. The knob exists now, so the remaining cost is a decision per type rather than new code. | A feature that needs a base type to engage across height. | 2026-08-09 |
| TD-002 | No committed test harness. Every verification rebuilds the `vm` sandbox from scratch. | Repeated setup cost and inconsistent coverage per change. | When a second feature needs behavioural proof. | 2026-08-09 |
| TD-003 | `Combat.applyDamage`'s return value means "clean hit", not "damage dealt" (INV-9) — a name that invites misuse. | Every new caller is a latent bug. | Any refactor of `js/combat/hitbox.js`. | 2026-08-09 |
| TD-005 | `Levels.ladder`'s doc comment cites "the level-authoring rules in the EO-001 architecture (§10)" — a document that no longer exists. The rules now live in `project-context/architecture.md` §Ladder authoring rules. | A reader follows a dead reference and concludes the rules were lost. | Next feature touching `js/levels/level.js`. | 2026-08-09 |
| TD-006 | No entity can walk past a solid: `Enemy.walk` refuses a step into a wall, so the road's five blocks (46–118 px) cut `level01_city` into segments no melee enemy ever leaves. The player can stall any of them behind cover. | Every wave must be authored inside one segment, and "pursue anywhere" cannot be specified honestly — feature 003 met FR-011 only within its segment. | A feature needing pursuit across the level, or a second level with real terrain. Fix is an optional `stepUp` height in `moveAndCollide`, default 0 — a change to the path every entity and projectile runs, so it needs its own feature. | 2026-08-18 |
| TD-007 | The beast's `scale` is capped by the sword's reach, not by art. Swings start at chest height, so the band where a swing connects narrows as the body grows — at 2.15 it closed to 0.6 px and the beast swung through the player. Held at 2.00. | Any future large melee type rediscovers this the same way. A bigger beast needs reach expressed per type, not a taller body. | A second oversized melee enemy, or any change to `Enemy.chase`'s preferred-range multiplier. | 2026-08-18 |
| ~~TD-004~~ | ~~Uncommitted `Levels.ladder()` residue.~~ **RESOLVED 2026-08-09** — it was feature 001's T001, and feature 001 shipped: commits `f46acd2`…`b5eeb58`, merged in PR #9. | — | closed | 2026-08-09 |
