<!-- QUICKSTART TEMPLATE · The scenarios a human actually runs to believe the
     feature works. Written so someone who did not build it can follow them.
     CAP 60 lines. -->
# Quickstart: {NAME}

**Spec:** `spec.md` · **Validates:** SC-1 … SC-n

## Setup
```
{the literal commands to get to a runnable state}
```

## Scenarios
### QS-1 — {title}  *(validates SC-1, US-1)*
1. {action}
2. {action}

**Expect:** {observable outcome, specific enough to fail}
**Fails if:** {the wrong outcome someone might mistake for success}

### QS-2 — {title}  *(validates SC-2)*
…

## Automated checks
```
{literal command}          # expect: {result}
```

## Not covered here
{What these scenarios do not prove, so nobody assumes they do.}
